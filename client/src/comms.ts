// let ws = undefined;

export function send(text: string) {
  console.log(`sending (not really): ${text}`);
  // if (ws && ws.readyState === WebSocket.OPEN) {
  //   ws.send(text);
  // } else {
  //   console.error("WebSocket is not open.");
  // }
}
